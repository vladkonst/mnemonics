{
  "permissions": {
    "allow": [
      "Bash(go version)",
      "Bash(git config --global user.name)",
      "Bash(git config --global user.email)",
      "Bash(git --version)",
      "Bash(git config --list --global)"
    ]
  }
}
